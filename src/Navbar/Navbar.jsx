import { NavLink } from "react-router-dom";

const Navbar = () => {
  return (
  
<nav>
      <div>
        <NavLink to="/" end>
        
        </NavLink>

      
      </div>
    </nav>
  );
};

export default Navbar;
